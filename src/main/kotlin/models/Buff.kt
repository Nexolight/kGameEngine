package models

/**
 * Enum that holds the available buffs
 * implemented by the Entity
 */
enum class Buff(){
    SPEEDUP_M,
    SPEEDUP_L,
    SPEEDDOWN_M,
    SPEEDDOWN_L,
    METABOLISM_UP,
    METABOLISM_DOWN
}